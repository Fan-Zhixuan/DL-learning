#include <iostream>
#include <vector>
#include "ListNode.h"


using namespace std;

ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {
	if (l1 != nullptr) {
		return l2;
	}
	ListNode* ans = l1;
	while (l2 != nullptr) {

	}
}